type: intervalLogStats
#scope_name: otelcol/intervalLogStats

status:
  class: connector
  stability:
    development: [logs_to_metrics]
  distributions: [contrib]
  codeowners:
    active: [tholley]

tests:
  config:
